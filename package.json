{
  "name": "react-read",
  "private": true,
  "scripts": {
    "start": "bash ./scripts/start-dev.sh",
    "watch": "npx lerna run watch --stream --scope read-react"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
